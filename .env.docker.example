# Docker environment settings
# Copy this file to .env.docker and modify as needed

# Security settings
# Generate a new secret key for production
SECRET_KEY=your-production-secret-key-here

# Set to False in production
DEBUG=False

# Set ALLOWED_HOSTS to a comma-separated list of allowed hosts
# Examples:
# - For development: localhost,127.0.0.1
# - For production: yourdomain.com,www.yourdomain.com
# - To allow any host (not recommended for production): *
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database settings
# The path where the SQLite database will be stored inside the container
DATABASE_DIR=/app/data

# Static files settings
# The path where static files will be collected inside the container
STATIC_ROOT=/app/staticfiles