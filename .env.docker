# Docker environment settings
# Generate a secure secret key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-production-secret-key-here
DEBUG=False
# Set ALLOWED_HOSTS to a comma-separated list of allowed hosts
# Use * to allow any host (not recommended for production)
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database settings
DATABASE_DIR=/app/data

# Static files settings
STATIC_ROOT=/app/staticfiles
